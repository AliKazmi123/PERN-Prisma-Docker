export type User = {
  id: number;
  name: string;
  email: string;
};

export type UserInput = {
  id: string;
  name: string;
  email: string;
};

export type NewUser = {
  name: string;
  email: string;
};
